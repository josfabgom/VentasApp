<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:VentasApp.ViewModels"
             x:Class="VentasApp.Views.CustomersPage"
             Title="{Binding Title}">

    <Grid RowDefinitions="Auto, *">
        <!-- Formulario de Alta/Edición -->
        <Frame Grid.Row="0" Margin="10" Padding="15">
            <VerticalStackLayout Spacing="10">
                <Label Text="{Binding CustomerId, Converter={StaticResource IntToTitleConverter}, ConverterParameter='Cliente'}" 
                       FontAttributes="Bold" FontSize="18" TextColor="{StaticResource Primary}"/>
                
                <Grid ColumnDefinitions="*,*" ColumnSpacing="10" RowDefinitions="Auto,Auto,Auto,Auto,Auto">
                    <VerticalStackLayout Grid.Row="0" Grid.Column="0">
                        <Label Text="Nombre" FontSize="12"/>
                        <Entry Text="{Binding Name}" Placeholder="Nombre completo"/>
                    </VerticalStackLayout>
                    
                    <VerticalStackLayout Grid.Row="0" Grid.Column="1">
                        <Label Text="Email" FontSize="12"/>
                        <Entry Text="{Binding Email}" Placeholder="email@ejemplo.com" Keyboard="Email"/>
                    </VerticalStackLayout>

                    <VerticalStackLayout Grid.Row="1" Grid.Column="0">
                        <Label Text="Teléfono / WhatsApp" FontSize="12"/>
                        <Entry Text="{Binding Phone}" Placeholder="Ej: +54 9 11..." Keyboard="Telephone"/>
                    </VerticalStackLayout>

                    <VerticalStackLayout Grid.Row="1" Grid.Column="1">
                        <Label Text="Tipo de Cliente" FontSize="12"/>
                        <Picker ItemsSource="{Binding AvailableTipos}" SelectedItem="{Binding Tipo}"/>
                    </VerticalStackLayout>

                    <VerticalStackLayout Grid.Row="2" Grid.ColumnSpan="2">
                        <Label Text="Dirección" FontSize="12"/>
                        <Entry Text="{Binding Address}" Placeholder="Calle y número"/>
                    </VerticalStackLayout>

                    <VerticalStackLayout Grid.Row="3" Grid.Column="0">
                        <Label Text="Localidad" FontSize="12"/>
                        <Entry Text="{Binding Localidad}" Placeholder="Ciudad/Pueblo"/>
                    </VerticalStackLayout>

                    <VerticalStackLayout Grid.Row="3" Grid.Column="1">
                        <Label Text="Provincia" FontSize="12"/>
                        <Entry Text="{Binding Provincia}" Placeholder="Provincia/Estado"/>
                    </VerticalStackLayout>

                    <VerticalStackLayout Grid.Row="4" Grid.Column="0">
                        <Label Text="Barrio" FontSize="12"/>
                        <Entry Text="{Binding Barrio}" Placeholder="Nombre del barrio"/>
                    </VerticalStackLayout>

                    <VerticalStackLayout Grid.Row="4" Grid.Column="1">
                        <Label Text="Zona" FontSize="12"/>
                        <Entry Text="{Binding Zona}" Placeholder="Zona de ventas"/>
                    </VerticalStackLayout>
                </Grid>

                <HorizontalStackLayout Spacing="10" HorizontalOptions="End" Margin="0,10,0,0">
                    <Button Text="Limpiar" 
                            Command="{Binding ClearFieldsCommand}" 
                            BackgroundColor="{StaticResource Gray400}"
                            TextColor="White"/>
                    
                    <Button Text="Guardar Cliente" 
                            Command="{Binding SaveCustomerCommand}" 
                            BackgroundColor="{StaticResource Success}"/>
                </HorizontalStackLayout>
            </VerticalStackLayout>
        </Frame>

        <!-- Lista de Clientes -->
        <RefreshView Grid.Row="1" Command="{Binding LoadCustomersCommand}" IsRefreshing="{Binding IsBusy}">
            <CollectionView ItemsSource="{Binding Customers}" SelectionMode="None" Margin="10">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <SwipeView>
                            <SwipeView.RightItems>
                                <SwipeItems>
                                    <SwipeItem Text="Editar" 
                                               BackgroundColor="{StaticResource Info}"
                                               Command="{Binding Source={RelativeSource AncestorType={x:Type viewModels:CustomersViewModel}}, Path=EditCustomerCommand}"
                                               CommandParameter="{Binding .}"/>
                                    <SwipeItem Text="Eliminar" 
                                               BackgroundColor="{StaticResource Secondary}"
                                               Command="{Binding Source={RelativeSource AncestorType={x:Type viewModels:CustomersViewModel}}, Path=DeleteCustomerCommand}"
                                               CommandParameter="{Binding .}"/>
                                </SwipeItems>
                            </SwipeView.RightItems>
                            
                            <Frame Margin="5" Padding="15">
                                <Grid ColumnDefinitions="*, Auto, Auto">
                                    <VerticalStackLayout Grid.Column="0">
                                        <Label Text="{Binding Name}" FontAttributes="Bold" FontSize="16"/>
                                        <Label Text="{Binding Phone}" FontSize="13" TextColor="{StaticResource Primary}"/>
                                        <Label Text="{Binding Address}" FontSize="12" TextColor="Gray"/>
                                        <HorizontalStackLayout Spacing="5">
                                            <Label Text="{Binding Localidad}" FontSize="12" TextColor="Gray"/>
                                            <Label Text="-" FontSize="12" TextColor="Gray"/>
                                            <Label Text="{Binding Barrio}" FontSize="12" TextColor="Gray"/>
                                            <Label Text="-" FontSize="12" TextColor="Gray"/>
                                            <Label Text="{Binding Zona}" FontSize="12" TextColor="Gray"/>
                                        </HorizontalStackLayout>
                                    </VerticalStackLayout>
                                    
                                    <!-- Botones de Acción Directos -->
                                    <HorizontalStackLayout Grid.Column="1" VerticalOptions="Center" Spacing="5" Margin="10,0">
                                        <Button Text="🖊️" 
                                                Padding="0"
                                                HeightRequest="35" WidthRequest="35"
                                                BackgroundColor="{StaticResource Info}"
                                                Command="{Binding Source={RelativeSource AncestorType={x:Type viewModels:CustomersViewModel}}, Path=EditCustomerCommand}"
                                                CommandParameter="{Binding .}"/>
                                        
                                        <Button Text="🗑️" 
                                                Padding="0"
                                                HeightRequest="35" WidthRequest="35"
                                                BackgroundColor="{StaticResource Secondary}"
                                                Command="{Binding Source={RelativeSource AncestorType={x:Type viewModels:CustomersViewModel}}, Path=DeleteCustomerCommand}"
                                                CommandParameter="{Binding .}"/>
                                    </HorizontalStackLayout>

                                    <Frame Grid.Column="2" 
                                           BackgroundColor="{AppThemeBinding Light={StaticResource Gray100}, Dark={StaticResource Gray900}}" 
                                           BorderColor="Transparent"
                                           Padding="8,4" 
                                           VerticalOptions="Start">
                                        <Label Text="{Binding Tipo}" FontSize="10" FontAttributes="Bold" TextColor="{StaticResource Primary}"/>
                                    </Frame>
                                </Grid>
                            </Frame>
                        </SwipeView>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
                <CollectionView.EmptyView>
                    <Label Text="No hay clientes" HorizontalOptions="Center" VerticalOptions="Center"/>
                </CollectionView.EmptyView>
            </CollectionView>
        </RefreshView>
    </Grid>

</ContentPage>
